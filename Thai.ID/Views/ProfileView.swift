import SwiftUI

struct ProfileView: View {
    @Binding var path: NavigationPath

    var body: some View {
        VStack {
            Button(String("profile")) {
                //
            }
        }.background(neutral01)
    }
}
