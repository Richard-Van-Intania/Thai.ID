import SwiftUI

struct HistoryView: View {
    @Binding var path: NavigationPath

    var body: some View {
        VStack {
            Button(String("history")) {
                //
            }
        }.background(neutral01)
    }
}
